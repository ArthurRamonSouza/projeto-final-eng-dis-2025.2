info:
  name: redis-pool-circuit
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrlEngine}}/health/redis-pool-circuit"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
