info:
  name: last-job-status
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8001/internal/last-job-status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
